#!/bin/zsh
tmux new-session -d -s default -n main
# Create window 9 with btop as initial command to avoid venv activation hook interference
tmux new-window -t default:9 -n btop btop
tmux select-window -t default:1
tmux attach-session -t default
